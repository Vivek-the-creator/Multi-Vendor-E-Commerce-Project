export async function GET() {
  return Response.json({ message: 'Auth route is handled by the custom CampusConnect session endpoints.' }, { status: 404 });
}

export async function POST() {
  return Response.json({ message: 'Auth route is handled by the custom CampusConnect session endpoints.' }, { status: 404 });
}
